import { AutoScrollToBottomDirective } from './auto-scroll.directive';

describe('AutoScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new AutoScrollToBottomDirective();
    expect(directive).toBeTruthy();
  });
});
